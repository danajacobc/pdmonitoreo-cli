export interface UserDTO {
    email: String;
    password: String;
}

export interface LoggedUserDTO {
  id:       string;
  username: string;
  lastname: string;
  email:    string;
  role:     string;
}
